import { createBrowserHistory } from "history";

//history se sada jednom kreira i prosledjuje se svakoj komponenti u kojoj je potrebno uraditi
//push ili pop (promenu ekrana odnosno promenu rute)
export default createBrowserHistory();
